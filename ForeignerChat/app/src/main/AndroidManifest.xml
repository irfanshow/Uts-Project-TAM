<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foreignerchats">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/foreignerchat_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/foreignerchat_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ForeignerChats">
        <activity
            android:name=".activity.LoginActivity"
            android:parentActivityName=".activity.StartActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.AboutActivity" />
        <activity
            android:name=".activity.RegisterActivity"
            android:parentActivityName=".activity.StartActivity" />
        <activity
            android:name=".activity.StartActivity"
            android:parentActivityName=".activity.splash" />
        <activity
            android:name=".activity.splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>